# Test-only dependencies. Deliberately NOT in requirements.txt: the Docker image installs only
# runtime deps, and nothing here is imported by the app.
#   .venv\Scripts\pip install -r requirements-dev.txt   (Windows)
#   .venv/bin/pip install -r requirements-dev.txt       (Linux)
pytest>=8.0
# tests/test_web.py drives the dashboard through FastAPI's TestClient, which is httpx underneath.
# The dashboard's own stack is requirements-web.txt (install that too to run those tests).
httpx>=0.27
